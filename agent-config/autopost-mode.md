# autopost-mode

mode: review

<!--
Controls /autopost and the daily job:

- review     = Kedin DRAFTS a post (and diagram) and sends it for approval. It does NOT publish.
               The user reviews, then /post to publish, or gives feedback (recorded into
               feedback-dataset.md). Recommended default until Kedin has learned the user's voice.
- autonomous = Kedin drafts AND publishes directly, with no approval.

Flip with /reviewmode or /autonomous (Kedin edits this line). The daily-autopost skill reads it each run.
-->
